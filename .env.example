# Clojure API (apps/api)
PORT=3000
MONGODB_URI=mongodb://localhost:27017/tododb
# REQUIRED: change this before running — any random string works locally
JWT_SECRET=replace-with-a-long-random-string-before-any-run

# React web (apps/web) — Vite reads VITE_* vars
VITE_API_URL=http://localhost:3000
